//displays all matched names in pairs
//has tabs at top for round one, round two, round 3
//each tab displays the same layout

export default function Match() {
    return (
        <div>
            <h1>Match Page</h1>
        </div>
    )
}